# این Workflow به صورت خودکار تغییرات در پوشه viraai/docs را از مخزن docs
# به مخزن خصوصی viraai-cloud-project سینک (Push) می‌کند.

name: Document Sync to Cloud Project

on:
  push:
    branches:
      - main # یا master، بر اساس نام برنچ اصلی شما
    paths:
      - 'viraaiio/docs/**' # فقط در صورت تغییر در این مسیر فعال شود

jobs:
  sync_docs:
    runs-on: ubuntu-latest

    steps:
      # 1. Checkout مخزن اصلی (docs) که تریگر شده است
      - name: Checkout Source Repository (viraaiio/docs)
        uses: actions/checkout@v4
        with:
          path: source_repo
          # از توکن پیش‌فرض Actions برای Checkout مخزن مبدا استفاده می‌کنیم
      
      # 2. Checkout مخزن مقصد (cloud-project)
      - name: Checkout Target Repository (viraai-cloud-project)
        uses: actions/checkout@v4
        with:
          repository: viraaiio/viraai-cloud-project # آدرس کامل مخزن مقصد
          token: ${{ secrets.PAT_TOKEN }} # اینجا از Secret شما استفاده می‌کنیم
          path: target_repo
          ref: master # برنچ مقصد (بر اساس تصویر، master است)

      # 3. کپی و سینک فایل‌ها
      - name: Copy & Sync files from Docs to Cloud Project Subdir
        run: |
          # ایجاد دایرکتوری مقصد در مخزن target
          mkdir -p target_repo/viraai/docs
          
          # کپی کردن تمام محتویات پوشه docs از source به target
          rsync -av --exclude='.git' source_repo/viraaiio/docs/ target_repo/viraai/docs/

      # 4. تنظیمات Git برای Push خودکار
      - name: Configure Git for Auto-Commit
        run: |
          git config --global user.email "actions@github.com"
          git config --global user.name "GitHub Actions Bot"
          
      # 5. Commit و Push تغییرات به مخزن مقصد
      - name: Commit and Push Changes to Target Repo
        run: |
          cd target_repo
          git add .
          
          # بررسی می‌کنیم آیا تغییری برای Commit وجود دارد یا نه
          if git diff --cached --exit-code; then
            echo "No changes to commit. Skipping push."
          else
            git commit -m "🤖 [Docs Sync] Automatic documentation update from viraaiio/docs"
            
            # استفاده از توکن PAT برای Push
            git push https://${{ secrets.PAT_TOKEN }}@github.com/viraaiio/viraai-cloud-project.git HEAD:master
          fi
        env:
          PAT_TOKEN: ${{ secrets.PAT_TOKEN }} # توکن را در محیط (Env) قرار می‌دهیم تا git push از آن استفاده کند.
